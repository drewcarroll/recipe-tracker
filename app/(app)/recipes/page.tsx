/**
 * Recipes tab (idea.md §2). The recipe list and editor land in later tasks;
 * this is the routed placeholder.
 */
export default function RecipesPage(): JSX.Element {
  return (
    <section className="tab-page">
      <h1 className="tab-title">Your Recipes</h1>
      <p className="tab-subtitle">Your recipe collection will live here.</p>
    </section>
  );
}
